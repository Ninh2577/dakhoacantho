# Chặn tất cả truy cập HTTP trực tiếp vào thư mục storage/
# File này bảo vệ: logs/, framework/, backups/ khỏi bị lộ công khai
Options -Indexes
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
